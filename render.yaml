services:
  - type: web
    name: bitely-api
    runtime: node
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      # Signiert die Login-Tokens. Render erzeugt beim ersten Deploy einen
      # zufälligen Wert — ohne ihn startet der Server nicht (siehe auth.ts).
      # Wird er später geändert, sind alle offenen Sitzungen ungültig.
      - key: JWT_SECRET
        generateValue: true
      # Google-Anmeldung für Gäste. Ohne Wert bleibt der Google-Knopf aus und
      # es geht nur E-Mail mit Passwort — die Anwendung startet trotzdem.
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: PORT
        value: 4000
